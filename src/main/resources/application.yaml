spring:
  application:
    name: store-admin
  jpa:
    show-sql: true
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://localhost:3306/word?useSSL=false&useUnicode=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root1234!!
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
  
  # 자이썬 파일 경로 지정
  jvm:
    arguments: "-Dpython.home=C:/path/to/python-libs"


token:
  secret:
    key: SpringBootJWTHelperTokenSecretKeyValue123MyWordProject!@#
  access-token:
    plus-hour: 1
  refresh-token:
    plus-hour: 12

# Python 스크립트 경로 설정
python:
  script:
    path: C:/Users/samsung/Desktop/coding_related_files/python/python/


# 네이버 웹 서치 id, 시크릿 키
x:
  naver:
    client:
      id: XXYzwuc0MJGce_K6Vo39
      secret: n0RAMiERNu
